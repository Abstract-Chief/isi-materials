## 📚 Полный Конспект Материалов

Ниже представлено структурное изложение предоставленного текста.

-----

### Цвик 1: Основы ML и Decision Tree

  * **SVC (Support Vector Classification):**
      * `svc = SVC(kernel='linear')`
      * **Линейное ядро** хорошо подходит для упорядоченных данных, когда нет больших отклонений от линии тренда.
  * **Разделение Датасета:**
      * `X_train, X_test, y_train, y_test = train_test_split(faces.data, faces.target, test_size=0.25, random_state=0)`
      * Разделение датасета на часть для **обучения** и часть для **тестирования**, чтобы тестировать модель не на тех строках, на которых она обучалась.
  * **Общий Алгоритм для Всех Моделей:**
    1.  Получить датасет.
    2.  Разделить на часть обучения и часть для тестирования: `x_train, x_test, y_train, y_test = train_test_split(X, y, test_size=0.25, random_state=0)`.
    3.  Инициализировать модель: `model = DecisionTreeClassifier()`.
    4.  Обучить модель: `model.fit(x_train, y_train)`.
    5.  Сделать предсказание тестовых данных: `y_pred = model.predict(x_test)`.
    6.  Посчитать точность предсказания: `accuracy_score(y_test, y_pred)`.
  * **DecisionTreeClassifier (Дерево Решений):**
      * Алгоритм машинного обучения, который строит дерево решений для классификации объектов.
      * **Параметры:**
          * `max_depth`: Чем меньше, тем "глупее" модель.
          * `min_samples_split`: Чем меньше, тем глубже уходит дерево; чем больше, тем дерево решений шире, соответственно, стабильнее.

-----

### Цвик 2: Предобработка Данных (Titanic)

  * **Работа с Массивом Данных (Titanic):**
      * `titanic_data = pd.read_csv('data/titanic.txt')`
      * `titanic_y = np.array(titanic_data.survived)` — Получение всех данных и целевой колонки.
  * **Удаление Колонок:**
      * `titanic_reduced = titanic_data.drop(columns=["row.names","survived","name","embarked", "home.dest","room", "ticket", "boat"])` — Удаление ненужных колонок.
  * **Обработка Возраста (Пропуски):**
      * `ages=titanic_reduced.iloc[:, 1]` — Получение колонки возраста.
      * `mean_age = np.mean(ages[~np.isnan(ages)])` — Нахождение среднего возраста (исключая `nan`).
      * `ages[np.isnan(ages)]=mean_age` — Присвоение всем возрастам со значением `nan` среднего возраста.
      * `titanic_reduced["age"]=ages` — Вставка обработанных данных в таблицу.
  * **Кодирование Категориальных Признаков (Sex):**
      * `enc = LabelEncoder()`
      * `label_encoder = enc.fit(titanic_reduced["sex"])`
      * `titanic_reduced["sex"] = label_encoder.transform(titanic_reduced["sex"])` — Нахождение всех разных значений (`DISTRICT`) в колонке, сортировка по алфавиту и преобразование в числовой формат.
  * **Проблема с Классом (One-Hot Encoding):**
      * Число класса (1, 2, 3) воспринимается моделью как упорядоченная величина, что некорректно.
      * Необходимо разбить на 3 колонки (например, `class 1 | class 2 | class 3`), где **1** будет отмечать, какой это класс, а **0** — все остальные.
  * **Финал Цвика 2:**
      * Очищенный датасет готов к загрузке в модель: `X_train, X_test, y_train, y_test = train_test_split(titanic_reduced, titanic_y, test_size=0.25, random_state=33)`.
      * Создание классификатора: `clf = tree.DecisionTreeClassifier(criterion='entropy', max_depth=6, min_samples_leaf=10)`.
      * Далее можно предсказывать смертность.

-----

### ДЗ 2 Цвика: Кластеризация, Валидация, Выбор Признаков

  * **Кластеризация K-Means:**
      * Делит данные на $K$ групп (кластеров) так, чтобы:
          * Точки внутри одной группы были максимально **похожи** друг на друга.
          * Точки из разных групп — максимально **отличались**.
      * **Elbow Method (Метод Локтя):** Метод для определения оптимального количества кластеров $K$.
          * $K$ слишком маленькое $\rightarrow$ группы будут слишком большими и размытыми.
          * $K$ слишком большое $\rightarrow$ модель усложняется и перестаёт быть полезной.
      * **Distortion:** Сумма квадратов расстояний всех точек до своих кластерных центров. Чем меньше distortion $\rightarrow$ тем лучше кластеризация.
  * **Кросс-Валидация (Cross-Validation):**
      * Способ проверить, насколько хорошо модель **обобщает** данные (работает не только на обучающих данных).
      * **Как работает (K-Fold):**
        1.  Разделение датасета на $K$ частей.
        2.  Выбор первой части для **теста**, остальные — для **обучения**.
        3.  Обучение и тестирование.
        4.  Повторение для каждой части (каждая часть становится тестовой в одной из итераций).
        5.  Получение **стабильной оценки**.
        <!-- end list -->
          * *Итерации (пример с $K=5$):*
              * Итерация 1: [TEST][TRAIN][TRAIN][TRAIN][TRAIN]
              * Итерация 2: [TRAIN][TEST][TRAIN][TRAIN][TRAIN]
              * ...
              * Итерация 5: [TRAIN][TRAIN][TRAIN][TRAIN][TEST]
  * **Выборка Данных (Feature Selection):**
      * **Удаление Мусора (VarianceThreshold):**
          * `selector = VarianceThreshold(threshold=0.01)`
          * Удаляются все **постоянные колонки** — те, в которых значения либо полностью одинаковые, либо почти не меняются между строками.
      * **Выборка Важных Данных (SelectFromModel):**
          * Создается классификатор (например, `RandomForestClassifier`).
          * `selector = SelectFromModel(clf)`
          * Селектор обучается (`selector.fit(X, y)`), а затем данные трансформируются (`X = selector.transform(X)`) для выбора наиболее важных признаков, определенных моделью.
      * **RFECV (Recursive Feature Elimination with Cross-Validation):**
          * **RFECV:** Рекурсивное исключение признаков с кросс-валидацией.
          * Обучает модель на всех признаках.
          * Удаляет **наименее важный**.
          * Обучает снова и удаляет ещё. И так до 1 признака.
          * На каждом шаге делает **кросс-валидацию**.
          * Находит точку, где модель показывает **лучший итоговый скор**.

-----

### Цвик 5: Регрессия и Алгоритмы Поиска

  * **Регрессия (Make Regression):**
      * `X, y = make_regression(1000, shuffle=True, random_state=None)` — Создает искусственный набор данных для **регрессии**.
      * **Регрессия** означает, что целевая переменная ($y$) — это **число** (не класс), и задача модели — предсказывать числовое значение (например, цену дома или вес по росту).
  * **Алгоритмы в Решении Number Puzzle (Поиска):**
      * **BFS (Breadth-First Search, Поиск в Ширину):** Проводится проверка состояний, удаленных от центра, **постепенно** — сначала все на расстоянии одного шага, потом все на расстоянии двух шагов, и так далее.
      * **DFS (Depth-First Search, Поиск в Глубину):** Идет до конца, пока не упрется или не дойдет до лимита (закрутится).
      * **Greedy Search (Жадный Поиск):** Выбирает каждый раз **ближайший к ответу ход** (локально оптимальный). Делает это исходя из некоей **функции оценки (эвристики)**, которая разная в каждой задаче.

[Image of BFS vs DFS search tree]

```
* **A\* (A-star):** Работает так же, как Greedy search, но при выборе хода **учитывает дальность от изначального состояния** (стоимость пути).
```

-----

### Судоку: Ограничения и Эвристики

  * **Домена (Domain):** Группа полей, в которых каждое влияет на состояние остальных (в Судоку это ряд, столбец и 9 полей).
  * **FC (Forward Checking):** После постановки числа сразу удаляет это число из **Домены** (возможных значений соседних клеток).
      * Если у какой-то клетки остаётся пустой набор значений $\rightarrow$ **Откат (Backtrack)**.
  * **MRV (Minimum Remaining Values):** Эвристика, выбирающая клетку, где **меньше всего возможных значений**. (Например, если клетка $X$ может быть только **9**, у нее меньше всего возможных значений).
  * **LCV (Least Constraining Value):** Эвристика, выбирающая клетку, которая **меньше всего влияет на соседние домены**.

-----

### Преднашка 7: Агенты

  * **Агент (Agent):** Сущность, которая **воспринимает** и **реагирует** на изменения в пространстве с помощью **сенсоров (внемков)** и **влияет** на среду посредством **акций (действий)**.

-----

### 3\. Различия Между UCS и A\* Алгоритмами

#### Акцент на Различия между UCS и A\* (Uniform Cost Search и A\*)

| Характеристика | UCS (Uniform Cost Search, Поиск с Единообразной Стоимостью) | A\* (A-star) |
| :--- | :--- | :--- |
| **Цель** | Найти кратчайший путь, измеряемый **стоимостью** (cost) пути от старта до текущего узла. | Найти кратчайший путь с учетом **стоимости** пути и **предполагаемого расстояния** до цели. |
| **Функция Оценки** | Использует только функцию стоимости пути $g(n)$. | Использует функцию $f(n) = g(n) + h(n)$. |
| **$g(n)$ (Cost to date)** | **Стоимость пути** от начального узла до текущего узла $n$. | **Стоимость пути** от начального узла до текущего узла $n$. |
| **$h(n)$ (Heuristic)** | **Не используется**. | **Эвристическая оценка** стоимости пути от текущего узла $n$ до целевого узла (оценка "насколько мы близки к цели"). |
| **Принцип Работы** | Всегда расширяет узел с **наименьшей общей стоимостью $g(n)$**. | Всегда расширяет узел с **наименьшей оценкой $f(n)$**. |
| **Эффективность** | **Медленнее**, чем A\*, так как слепо ищет по стоимости, не имея представления о направлении к цели. | **Значительно быстрее** (в большинстве случаев), так как эвристика направляет поиск к цели. |
| **Гарантия Оптимальности** | **Да** (при неотрицательных весах). | **Да** (при условии, что эвристика $h(n)$ является **допустимой** (admissible) и **монотонной/согласованной** (consistent)). |

  * **UCS** — это, по сути, частный случай A\*, где эвристическая функция $h(n)$ всегда равна нулю ($h(n)=0$). UCS гарантирует нахождение пути с наименьшей стоимостью, но может оказаться очень неэффективным, так как исследует все пути с меньшей стоимостью, даже если они ведут в неверном направлении.
  * **A\*** использует эвристику $h(n)$ для "направления" поиска, выбирая узлы, которые, по его оценке, находятся ближе к цели, что делает его гораздо более **целенаправленным** и **быстрым** при сохранении оптимальности.
